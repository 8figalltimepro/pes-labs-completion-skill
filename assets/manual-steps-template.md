# Manual Steps for <Lab Info>

Do these steps yourself after the agent finishes its part. The agent
never performs user-side steps: no screenshots, no portal or tracker
updates, no submissions.

## Steps to take

1. <One numbered entry per user-side step, in order. Examples: take a
   screenshot, move a JIRA ticket, fill a submission form, upload a
   file. Each entry states exactly what to do and how to confirm it
   is done.>

## Screenshots (only if the assignment deliverables include figures)

Save all files into the `screenshots/` folder in the lab folder.
Use PNG format for every shot.

### What to leave out of every shot

Crop to the output only. Leave out the browser bar, personal tabs,
address bar, bookmarks, file path, and the editor sidebar.
Only the plot or the text output should be visible.

### Shots to take

1. <Which cell or command to run. Take a shot of the output only.
   Include the key regions.
   Save as `screenshots/<NN_snake_case.png>`.>

2. <Repeat one numbered entry per shot. If one output block does not
   fit in a single clear shot, use one shot per part with letter
   suffixes (`06a`, `06b`) and update every tex image tag to match.>

## When done

Check that every expected file or update from the steps above is in
place. Open each screenshot and check it is sharp and cropped to the
output only. Then tell the engineer everything is ready so the final
build can start. The PDF is built only after you confirm.
