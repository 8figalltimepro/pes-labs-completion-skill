# <Lab Info> Plan

## Context

<2 to 4 sentences: the literal assignment, what done looks like, and the final deliverables.>

## Approach

### Step A. <First code change>

Target: <exact file, cell, or function>.

<Exact edit: what to add or replace and the literal new code or behavior.>

### Step B. <Next change>

<Repeat the same shape for every change. One step per behavior.>

### Step C. Run and record real numbers

Run the full program top to bottom in one session. Fix only runtime errors, never restyle. Copy the real printed outputs into a temp note. These numbers feed the report table. Never invent or guess.

### Step D. Create manual_steps.md

Checklist of every screenshot with exact file names under `screenshots/`. See the manual-steps template.

### Step E. Create report.tex with image tags

Report structure, results table fed by Step C numbers, and image tags matching the Step D names exactly.

### Step F. Wait for screenshot confirm, then verify and build PDF

Do not build before the user confirms `screenshots/` is filled. Then check all names exist, compile to `<Lab>_Report.pdf`, and confirm every required part shows. Never write over the assignment brief file.

## Critical Files and Anchors

- <Path>: <region plus one-line reason.> (At most 5 entries.)

## Verification

- <Exact command plus expected output for each risky step.>

## Assumptions and Contingencies

- <Only user-overridable decisions plus fallbacks in if-X-do-Y form.>
